version: 2

models:
  - name: stg_postgres__products
    description: "product info"
    columns:
      - name: product_id
        description: "the primary key for this table"
        tests:
          - unique
          - not_null
