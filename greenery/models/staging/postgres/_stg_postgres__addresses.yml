version: 2

models:
  - name: stg_postgres__addresses
    description: "custom addresses"
    columns:
      - name: address_id
        description: "the primary key for this table"
        tests:
          - unique
          - not_null
