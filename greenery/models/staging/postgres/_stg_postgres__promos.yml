version: 2

models:
  - name: stg_postgres__promos
    description: "product promotions"
    columns:
      - name: promo_id
        description: "the primary key for this table"
        tests:
          - unique
          - not_null
